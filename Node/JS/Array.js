/*
    push()                          add a data in end of array
    pop()                           delete a data in end of array
    shift()                         delete a data in head of array
    unshift()                       add a data in head of array
    splice(index,number)            return value of delete
    concat()                        combine
    split()                         str2arr
    sort()                          return array of sort
    slice(index,index)              return value of delete
    forEach(callback)               like for statement.no return                                                        false
    map(callback)                   change array and return. have return.
    filter(callback)                return                                                                              false
    every(callback)                 if satisfy condition ,return boolean --> arr(boolean)                               false
    some()                          if satisfy condition ,return boolean --> value                                      false
    reduce(callback,initialValue)   *****                                                                               false
    Array.from()                    change pseudoArray to array
    Array.of()                      change everything to array
    copyWithin(target start end)    copy array. return new array
    find(callback)                  return value of find                                                                false
    findIndex(callback)             return index of find                                                                false
    fill(value,start,end)           fill value in special position of array
    includes()                      return value of boolean
    indexOf()                       return value of index
    keys()                          return keys of array
    values()                        return values of array
    entries()                       return keys and values of array
*/